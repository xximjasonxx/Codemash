<Page
    x:Class="Codemash.Client.Views.MapView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource ApplicationBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="110"/>
            <RowDefinition Height="44*"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.ColumnSpan="2" Grid.RowSpan="2">
            <Image Source="/Assets/ConventionCenterFinal.png" Stretch="UniformToFill" />
        </ScrollViewer>

        <Border VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Row="0" Grid.Column="0">
            <Button x:Name="GoBack" Style="{StaticResource CMBackButton}" VerticalAlignment="Center"
                    HorizontalAlignment="Center" />
        </Border>
    </Grid>
</Page>