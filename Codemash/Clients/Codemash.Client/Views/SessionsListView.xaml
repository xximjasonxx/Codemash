<app:LayoutAwarePage
    x:Class="Codemash.Client.Views.SessionsListView"
    xmlns:app="using:Codemash.Client.Core"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:i="using:Windows.UI.Interactivity"
    xmlns:cal="using:Caliburn.Micro" mc:Ignorable="d">

    <Page.Resources>
        <app:SessionItemTemplateSelector x:Name="templateSelector" />
        <app:SessionItemContainerStyleSelector x:Name="containerStyleSelector" />
    </Page.Resources>

    <Grid Background="{StaticResource ApplicationBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="100"/>
            <ColumnDefinition Width="221*"/>
            <ColumnDefinition Width="1010*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button x:Name="backButton" Click="GoBack" IsEnabled="{Binding Frame.CanGoBack, ElementName=pageRoot}"
                Style="{StaticResource CMBackButton}" Margin="36,0,0,36" />

        <!-- Page Title TextBlock -->
        <TextBlock Text="All Sessions" Style="{StaticResource PageTitle}" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="0"
                       VerticalAlignment="Center" Height="67" Margin="0,18" />

        <!-- Grid View Block -->
        <Border Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2" Margin="10,10,10,0" Padding="20,10">
            <GridView SelectionMode="None" IsItemClickEnabled="True" ItemTemplateSelector="{StaticResource templateSelector}"
                      ItemsSource="{Binding ListSource}" ItemContainerStyleSelector="{StaticResource containerStyleSelector}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="ItemClick">
                        <cal:ActionMessage MethodName="SessionClick">
                            <cal:Parameter Value="$eventArgs" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VariableSizedWrapGrid />
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
            </GridView>
        </Border>
    </Grid>
</app:LayoutAwarePage>